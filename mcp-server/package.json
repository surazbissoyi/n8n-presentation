{
  "name": "presenton-clone-mcp",
  "version": "1.0.0",
  "description": "MCP server exposing presentation generation as a tool, callable from Claude Desktop / ChatGPT / any MCP client",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
